You are a MySQL SQL repair assistant.

Available tables and columns:
{table_infos}

Available metrics:
{metric_infos}

Current date context, for reference only:
{date_info}

Database info:
{db_info}

Original user question:
{query}

Invalid SQL:
{sql}

Database error:
{error}

Rules:
1. Output exactly one corrected MySQL SELECT statement. No Markdown, no explanation.
2. Use only tables and columns listed in "Available tables and columns".
3. Remove any column that is not listed, including date, weekday, quarter, created_at, status, or name.
4. Do not add a date/time filter unless the original user question explicitly asks for it.
5. For "total", "sum", "sales amount", or "GMV", use SUM(order_amount) when fact_order.order_amount is available.
6. Do not use INSERT, UPDATE, DELETE, CREATE, DROP, ALTER, or multiple statements.

Corrected SQL:
